# real credentials — .env.example is the tracked template
.env
.env.local

# dispatch local state: contextual-chunk cache, persisted memory, indexes
.dispatch/

# built binaries
/dispatch
/cmd/dispatch/dispatch
/minimal
/examples/minimal/minimal

# go
*.test
*.out

# macOS
.DS_Store
